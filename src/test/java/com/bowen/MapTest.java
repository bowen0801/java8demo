package com.bowen;

import org.junit.Test;

public class MapTest {
    @Test
    public void useMapInit(){
        /*
           1. Map初始化方式
        List<PharmacyGoodsDetailSelfVo> detailList = detailListMap.get(pharmacyId);
        Map<Long, Integer> shortageMap = Maps.newHashMap();
        multipleSplitQuantityMap.put(pharmacyId, Maps.newHashMap());
        multipleSplitQuantityMapExcludePresent.put(pharmacyId, Maps.newHashMap());*/
    }
}
